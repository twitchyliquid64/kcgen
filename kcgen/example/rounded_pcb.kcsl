load("shapes.lib", "shapes")
load("draw.lib", "draw")
load("pcb.lib", p="pcb")

#[print(x) for x in draw.pcb.outline(shapes.box(3,3, rounded_radius=1))]
#print(p.arc(layer = layers.edge))

pcb = PCB(
	# drawings=[p.text(content = "yolo")],
	drawings = [p.line()],
	segments = [p.via(XY(2,2))],
)
print(pcb.segments)